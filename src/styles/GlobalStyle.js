import {
    StyleSheet
  } from 'react-native';
const styles = StyleSheet.create({
  Container: {
      flex:1,
      flexDirection:'row',
      alignContent:'center',
      alignItems: 'center'
  },
  Input: {
      padding: 10,
      height:40,
      borderWidth:1
  }
});

export default styles;