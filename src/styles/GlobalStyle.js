import {
    StyleSheet
  } from 'react-native';
const styles = StyleSheet.create({
  Container: {
      flex:1,
      flexDirection:'row',
      alignContent:'center',
      alignItems: 'center'
  },
  InputStyle: {
    height: 40,
    width: 300,
    margin: 12,
    backgroundColor: 'white',
    borderWidth: .5,
    borderRadius: 5,
    padding: 10,
  }
});

export default styles;